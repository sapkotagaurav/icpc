#include<bits/stdc++.h>

 using namespace std; 

 int main(){
    int n, k;
    scanf("%d %d", &n, &k);
    vector<int> time(n);
    priority_queue<int, vector<int>, greater<int>> pq;
    for (int i = 0; i < n; i++)
    {
        cin>>time[i];
        pq.push(time[i]);
    }

    for (size_t i = 0; i < k; i++)
    {
        /* code */
    }
    
    

    




}